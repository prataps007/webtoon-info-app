<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Favorites Section -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_favorites"
            android:title="My Favorites"
            android:icon="@drawable/ic_favorite" />
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_watch_later"
            android:title="Watch Later"
            android:icon="@drawable/ic_watch_later" />
    </group>

    <!-- Featured Section -->
    <group android:label="Featured">
        <item
            android:id="@+id/nav_featured"
            android:title="Featured"
            android:icon="@drawable/ic_star" />
    </group>

    <!-- What to Watch Section -->
    <group android:label="Recommendations">
        <item
            android:id="@+id/nav_what_to_watch"
            android:title="What to Watch"
            android:icon="@drawable/ic_watch" />
    </group>

    <!-- Latest News and Reviews Section -->
    <group android:label="Updates">
        <item
            android:id="@+id/nav_latest_news"
            android:title="Latest News"
            android:icon="@drawable/ic_news" />
        <item
            android:id="@+id/nav_reviews"
            android:title="Reviews"
            android:icon="@drawable/ic_review" />
    </group>

    <group android:label="My Profile">
        <item
            android:id="@+id/nav_my_profile"
            android:title="My Profile"
            android:icon="@drawable/ic_user" />
    </group>

</menu>
